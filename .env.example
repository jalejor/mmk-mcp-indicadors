# =============================================================================
# CONFIGURACIÓN DE LA APLICACIÓN
# =============================================================================

# Información básica del servicio
DD_SERVICE=fastapi # Nombre del servicio
DD_VERSION=1.0.0   # Versión del servicio
APP_ID=fastapi     # Identificador único de la aplicación

# Configuración de entorno
PY_ENV=development     # Entorno de Python (development/production)
PYTHON_ENV=development # Alias para PY_ENV (mantener sincronizado con PY_ENV)
ASGI_ENV=local         # Entorno ASGI (local/docker)

# =============================================================================
# CONFIGURACIÓN DEL SERVIDOR
# =============================================================================

# Host y puerto del servidor
HOST=0.0.0.0 # Host del servidor (0.0.0.0 para Docker, localhost para desarrollo local)
PORT=8000    # Puerto del servidor

# =============================================================================
# CONFIGURACIÓN DE RUTAS
# =============================================================================

# Prefijo y rutas de la API
PREFIX_PATH=/prefix     # Prefijo base para todas las rutas de la API
HEALTHY_PATH=/healthy   # Ruta para health checks
LIVENESS_PATH=/liveness # Ruta para liveness probe

# =============================================================================
# CONFIGURACIÓN DE LOGGING
# =============================================================================

# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO # Nivel de detalle para los logs
