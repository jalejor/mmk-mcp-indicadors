# Minimal dependency set used to run the test suite inside Docker.
# Mirrors the runtime libraries actually exercised by the unit tests
# (indicators, rules, backtest) without dragging private SSH-cloned packages.
fastapi==0.115.14
uvicorn==0.35.0
pyyaml==6.0.2
ccxt==4.2.97
pandas==2.2.2
pandas-ta-classic==0.3.14b1
numpy<2.0.0
cachetools==5.3.3
slowapi==0.1.9
pytest==8.2.0
httpx==0.28.1
# Needed by the MCP auth test (public HTTPS fork, not SSH). Same pin as runtime.
fastapi-mcp @ git+https://github.com/am1ter/fastapi_mcp.git@e121a861a839d1beafd8e554a548f7b54a2e8af6
